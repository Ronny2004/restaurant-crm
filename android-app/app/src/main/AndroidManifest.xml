<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_app_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.RestaurantCRM"
        tools:targetApi="31">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.admin.AdminActivity" android:exported="false" />
        <activity android:name=".ui.waiter.WaiterActivity" android:exported="false" />
        <activity android:name=".ui.chef.ChefActivity" android:exported="false" />
        <activity android:name=".ui.cashier.CashierActivity" android:exported="false" />
        
        <!-- Admin Product Management -->
        <activity android:name=".ui.admin.products.ProductListActivity" android:exported="false" />
        <activity android:name=".ui.admin.products.AddEditProductActivity" android:exported="false" />
        <activity android:name=".ui.admin.sales.SalesHistoryActivity" android:exported="false" />
    </application>

</manifest>